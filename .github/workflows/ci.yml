name: FilaOps CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  # ============================================
  # Backend Tests (Python/FastAPI)
  # ============================================
  backend-tests:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: './backend/requirements.txt'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-cov

      - name: Run tests
        run: |
          pytest tests/ -v --cov=app --cov-report=xml --cov-report=term-missing
        env:
          # Use SQLite for testing (no SQL Server needed)
          DATABASE_URL: "sqlite:///:memory:"
          SECRET_KEY: "test-secret-key-for-ci-only"
          DEBUG: "true"

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./backend/coverage.xml
          fail_ci_if_error: false

  # ============================================
  # Backend Linting
  # ============================================
  backend-lint:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install linters
        run: |
          pip install ruff

      - name: Run Ruff
        run: ruff check app/ --output-format=github

      - name: Install type checking dependencies
        run: |
          pip install mypy types-python-dateutil
          pip install sqlalchemy[mypy]  # SQLAlchemy mypy plugin

      - name: Run MyPy type checking
        run: mypy app/ --config-file pyproject.toml || true
        continue-on-error: true  # Don't fail CI on type errors yet

  # ============================================
  # Frontend Tests (React/Vite)
  # ============================================
  frontend-tests:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './frontend/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Build check (development mode)
        run: npm run build
        env:
          VITE_API_URL: "http://localhost:8001"
          # Note: Production builds are disabled - see frontend/PRODUCTION_BUILD_BLOCKED.md

  # ============================================
  # Note: Docker builds removed - project now uses PostgreSQL directly
  # ============================================
